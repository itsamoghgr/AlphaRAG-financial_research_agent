"""Ingestion building blocks: ticker resolution, EDGAR fetch, parse, chunk, embed.

These are pure modules with no FastAPI imports. The orchestrator that uses
them lives in `services/ingestion_service.py`.
"""
