"""Services: orchestration / use cases.

Routers in `api/` may only call into this package. Services compose
repositories and the ingestion / retrieval / generation modules. They
themselves do no I/O directly.
"""
